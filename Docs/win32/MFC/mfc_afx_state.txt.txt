###########################################
# 直接用上了TLS, 线程自己的数据
	# class CProcessLocalObject
	{
		//第一次看到这种写法, 传入一个函数指针,用来创建对象
		//创建的对象存在这个类中,
		//在此类销毁时,销毁对象
	}

###########################################
# 框架层面定义了各种 全局state	
	# Module state
		# 你写的MFC程序,和别人写的MFC程序,如何组合到一个,2个不同的dll
		# 你写的MFC程序调用了别人的OLE控件,在OLE控件处,模块状态就会发生变化

		# DLL 和 EXE 都是module
	# 进程 state
	# 线程 state